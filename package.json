{
  "name": "gabriel-athanasiou-portfolio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "dev:vite": "vite",
    "dev:directing": "PORTFOLIO_MODE=directing npm run build:data && PORTFOLIO_MODE=directing npm run prepare:build && VITE_PORTFOLIO_MODE=directing vite",
    "dev:postprod": "PORTFOLIO_MODE=postproduction npm run build:data && PORTFOLIO_MODE=postproduction npm run prepare:build && VITE_PORTFOLIO_MODE=postproduction vite",
    "sync:static": "node scripts/sync-static-to-cloudinary.mjs",
    "sync:static:directing": "PORTFOLIO_MODE=directing node scripts/sync-static-to-cloudinary.mjs",
    "sync:static:postprod": "PORTFOLIO_MODE=postproduction node scripts/sync-static-to-cloudinary.mjs",
    "sync:directing": "PORTFOLIO_MODE=directing node scripts/sync-data.mjs",
    "sync:postprod": "PORTFOLIO_MODE=postproduction node scripts/sync-data.mjs",
    "sync:both": "node scripts/sync-data-both.mjs",
    "sync:all": "npm run sync:both && npm run build:static:all && npm run sync:static:directing && npm run sync:static:postprod",
    "upload:config-images": "node scripts/upload-config-images.mjs",
    "build:content": "node scripts/generate-share-meta.mjs",
    "build:data": "node scripts/sync-data.mjs",
    "build:sitemap": "node scripts/generate-sitemap.mjs",
    "build:robots": "node scripts/generate-robots.mjs",
    "build:static": "npm run build:sitemap && npm run build:content && npm run build:robots",
    "build:static:directing": "PORTFOLIO_MODE=directing npm run build:static",
    "build:static:postprod": "PORTFOLIO_MODE=postproduction npm run build:static",
    "build:static:all": "npm run build:static:directing && npm run build:static:postprod",
    "prepare:build": "node scripts/prepare-build.mjs",
    "build": "vite build",
    "build:full": "npm run build:data && npm run build:static && npm run sync:static && npm run prepare:build && vite build",
    "build:directing": "PORTFOLIO_MODE=directing npm run build:data && PORTFOLIO_MODE=directing npm run build:static && PORTFOLIO_MODE=directing npm run sync:static && PORTFOLIO_MODE=directing npm run prepare:build && VITE_PORTFOLIO_MODE=directing vite build",
    "build:postprod": "PORTFOLIO_MODE=postproduction npm run build:data && PORTFOLIO_MODE=postproduction npm run build:static && PORTFOLIO_MODE=postproduction npm run sync:static && PORTFOLIO_MODE=postproduction npm run prepare:build && VITE_PORTFOLIO_MODE=postproduction vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@netlify/functions": "^2.8.1",
    "@remix-run/router": "1.15.3",
    "airtable": "^0.12.2",
    "cloudinary": "^2.8.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "6.22.3"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "@vitest/ui": "^4.0.14",
    "dotenv": "^17.2.3",
    "netlify-plugin-cache": "^1.0.3",
    "sharp": "^0.34.5",
    "typescript": "~5.8.2",
    "vite": "^6.2.0",
    "vitest": "^4.0.14"
  }
}
