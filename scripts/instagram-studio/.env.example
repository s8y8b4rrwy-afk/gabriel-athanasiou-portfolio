# Instagram Studio Environment Variables

# Password protection (SHA-256 hash of your password)
VITE_PASSWORD_HASH=your_sha256_hash_here

# Netlify Functions base URL
# Recommended: leave empty to use relative URLs like "/.netlify/functions/*".
# Only set this if you need to point the Studio frontend at a specific host.
VITE_FUNCTIONS_BASE_URL=

# Optional override for the sync endpoint (normally not needed)
VITE_SYNC_FUNCTION_URL=

# Instagram OAuth (Phase 3)
VITE_INSTAGRAM_APP_ID=your_app_id
VITE_INSTAGRAM_REDIRECT_URI=https://studio.lemonpost.studio/auth/callback

# Optional override for the auth endpoint (normally not needed)
VITE_AUTH_FUNCTION_URL=

# -----------------------------
# Server-side (Netlify Functions)
# -----------------------------
# Add these in Netlify (Site configuration → Environment variables) for studio.lemonpost.studio
# Do NOT add VITE_ prefix for server-side secrets.

# Instagram / Meta app (required for auth + publish)
INSTAGRAM_APP_ID=your_meta_app_id
INSTAGRAM_APP_SECRET=your_meta_app_secret

# Cloudinary (required to read/write schedule JSON)
CLOUDINARY_CLOUD_NAME=date24ay6
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Optional: email notifications
RESEND_API_KEY=your_resend_api_key
NOTIFICATION_EMAIL=you@yourdomain.com

# Optional: safe rollout / testing mode
INSTAGRAM_DRY_RUN=true
