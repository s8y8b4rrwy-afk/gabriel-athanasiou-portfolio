/**
 * Feature Flags
 * 
 * Toggle experimental or new features here.
 * Set to false to revert to original behavior.
 */

export const FEATURES = {
  /**
   * Apple-style calendar on mobile devices
   * - Single-letter day headers (M, T, W...)
   * - Pill-shaped event indicators
   * - Red circle "today" indicator
   * - Floating "Today" button
   * - Hidden view toggle (month only)
   * 
   * Set to false to use the original calendar style on all devices.
   */
  APPLE_STYLE_CALENDAR: true,
};
